<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0/EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.board.mapper.ReplyMapper">

    <!-- 댓글 입력 -->
    <insert id="insertReply">
        INSERT INTO tb_reply (bno, replytext, replyer)
        VALUES (#{bno}, #{replytext}, #{replyer})
    </insert>

    <!-- 댓글 목록 -->
    <select id="listReply" resultType="com.example.board.domain.ReplyDTO">
        SELECT rno, bno, replytext, replyer, user_name AS userName, r.regdate, r.updatedate
        FROM tb_reply r, tb_user u
        WHERE  r.replyer = u.user_id AND bno=#{bno}
        ORDER BY rno
    </select>

</mapper>